version: '3.8'

services:
  libera-dev-db:
    platform: linux/amd64
    image: postgres:14
    restart: always
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=libera_master
      - POSTGRES_PASSWORD=masterpass
      - POSTGRES_DB=postgres
    volumes:
      - ./database/initdb:/docker-entrypoint-initdb.d

  flyway:
    platform: linux/amd64
    image: flyway/flyway:latest
    environment:
      - FLYWAY_PASSWORD=masterpass
      - FLYWAY_CONNECT_RETRIES=60
    command: -url=jdbc:postgresql://libera-dev-db:5432/libera_sdp_dev migrate
    volumes:
      - ./database/migrations:/flyway/sql
      - ./database/flyway.conf:/flyway/conf/flyway.conf
    depends_on:
      - libera-dev-db

  sdp:
    platform: linux/amd64
    image: libera-utils:latest
    build:
      context: .
      target: libera-utils

  tests:
    platform: linux/amd64
    image: libera-utils-test:latest
    build:
      context: .
      target: libera-utils-test
    environment:
      - LIBERA_DB_HOST=libera-dev-db
    depends_on:
      flyway:
        condition: service_completed_successfully

  docs-confluence:
    image: libera-utils-docs:latest
    build:
      context: .
      target: libera-utils-docs
    command: ["confluence"]

  docs-html:
    image: libera-utils-docs:latest
    build:
      context: .
      target: libera-utils-docs
    command: [ "clean", "html" ]

  linting:
    image: libera-utils-test:latest
    build:
      context: .
      target: libera-utils-test
    entrypoint: ["pylint", "libera_utils"]

  ast:
    image: libera-utils-test:latest
    build:
      context: .
      target: libera-utils-test
    entrypoint: [ "bandit", "-r", "libera_utils" ]