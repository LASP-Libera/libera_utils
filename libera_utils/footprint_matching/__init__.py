"""Footprint matching module for the Libera Science Data Center pipeline.

This subpackage contains the reader plugin system, shared data types, and
(in later milestones) the PSF engine, geometry module, tile manager, and
footprint orchestrator described in the FMATCH algorithm design.

Milestone 1 scope: reader plugin infrastructure only.
  - libera_utils.footprint_matching.types       — shared data classes and enums
  - libera_utils.footprint_matching.readers     — GriddedDataReader plugin system
"""
