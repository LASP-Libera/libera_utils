"""FMATCH-CAM algorithm runner.

This subpackage contains the manifest-driven processing runner (``fmatch_cam.py``) and container
``Dockerfile`` for the FMATCH-CAM data product: the radiometer-timescale, lowest-latency (camera /
near-real-time) footprint-matching product, which runs continuously from mission start.
"""
