"""FMATCH-CAM-CAMTIME algorithm runner.

This subpackage contains the manifest-driven processing runner (``fmatch_cam_camtime.py``) and
container ``Dockerfile`` for the FMATCH-CAM-CAMTIME data product: the camera-timescale,
lowest-latency (camera / near-real-time) footprint-matching product, which runs continuously from
mission start.
"""
