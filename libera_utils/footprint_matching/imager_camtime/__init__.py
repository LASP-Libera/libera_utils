"""FMATCH-IMAGER-CAMTIME algorithm runner.

This subpackage contains the manifest-driven processing runner (``fmatch_imager_camtime.py``) and
container ``Dockerfile`` for the FMATCH-IMAGER-CAMTIME data product: the camera-timescale product at
RBSP Climate Quality latency. It becomes available once RBSP products are flowing (post-Year 1).
"""
